[pytest]
# Unit tests live under Project/tests/unit/. The legacy scripts directly
# under Project/tests/ are hardware-diagnostic CLIs and are not collected.
testpaths = Project/tests/unit
pythonpath = Project
python_files = test_*.py
addopts = -v --tb=short -ra
filterwarnings =
    error
    ignore::DeprecationWarning
